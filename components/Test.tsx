import { GeoJSONSourceDiff, GeoJSONSourceMLGL, GeoJSONSourceSpecification } from "@maptiler/sdk";

export const Test = () => {
  const c: GeoJSONSourceSpecification = {
    type: "geojson",
    data: "asdf",
  };
};
